version: '2.4'

services:

  idilic:
    volumes: ['../../:/app']
    extends:
      file: base.yml
      service: idilic
    depends_on: [database]

  server:
    volumes: ['../../:/app']
    extends:
      file: base.yml
      service: server
    depends_on: [database]

  database:
    extends:
      file: base.yml
      service: database

volumes:
  schema:
    driver: local
