version: '2.4'

services:

  yjq:
    restart: always
    image:   ${REPO}/${PROJECT}.yjq:${TAG}
    build:
      context: ../../../
      dockerfile: ${REALDIR}infra/docker/tools/yjq.dockerfile
    volumes:
      - ${OUTMAKEDIR}:/app
